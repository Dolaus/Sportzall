@model IEnumerable<User>
@*@model IEnumerable<User>
@{
    ViewData["Title"] = "Home Page";
}

<div class="container p-3">

    <div class="row">
    @if (Model.Count()>0)
    {
                @foreach(var obj in Model)
                {
                   <div class="col-lg-4 col-md-6 pb-4 filter @obj.Name.Replace(' ','_')">
 
              <div class="card bg-white rounded shadow-sm" style="border:1px solid #222">
                <div>
                   <img class="card-img-top img-fluid d-block mx-auto mb-3" width="100%" src="@URL.ImageUserURL@obj.Image" alt="Card image cap">
                </div>
                <div class="card-body p-1 px-3 row">
                    <div class="col-7"><h4 class="float-left">@obj.Name</h4></div>
                <div class="col-12 pt-2" style="font-size:13px; text-align:justify" >
                    <h4>@obj.Year</h4>
                    <p>@obj.ShortInfo</p>
                </div>
                <div class="col-12 p-1">
                    <a asp-controller="User" asp-action="Details" asp-route-id="@obj.Id" class="btn btn-dark form-control btn-sm p-2" style="height:40px">View Details</a>
                </div>
                </div>
            </div>
        </div>
                }

    }
    else
    {
        <p> No Users </p>
    }
    </div>
</div>*@
 <body>
<main class="main">
    <section class="hero">
        <div class=" overlay">
            <div class="hero_content container">
                <div class="hero_info">
                    <span class="hero_span">This</span>
                    <h1 class="hero_title">Sport<span class="logo-accent">hub<span></h1>

                    <div class="hero_info">
                        <ul class="info-list list">
                            <li class="item">
                                    <h3 class="uptitle">ЗНАЙОМТЕСЬ З ФІТНЕС-ФЕНОМЕНОМ ПРЯМО З УКРАЇНИ!</h3>
                                    <p class="text">Під час першого візиту до нашого клубу буде проведено аналіз будови тіла та консультація тренера, який допоможе адаптувати тренування до ваших очікувань. Запишіться на свій перший безкоштовний тренінг з МС Петьов!</p>
                                <button class="button button-header">Зареєструватися</button>
                            </li>
                            <li class="item">
                                    <h3 class="uptitle">ПОДИВІТЬСЯ, ПРО ЩО НАВЧАННЯ</h3>
                                    <p class="text">Познайомтеся з нашою методикою навчання та результатами, яких ви досягнете завдяки їй! </p>
                                <button class="button button-header">See more</button>
                            </li>
                        </ul>
                    </div>
                </div>

    </section>
<br />
        <br />
    <section class="people">


            <h2 class="section-title">
                ЗНАЙОМТЕСЯ З НАМИ
                <span> ЦЕ НАША КОМАНДА </span>
            </h2>
            <div class="row">
                @if (Model.Count() > 0)
                {
                    @foreach (var obj in Model)
                    {
                        <div class="col-lg-4 col-md-6 pb-4 filter @obj.Name.Replace(' ','_')">

                            <div class="card bg-white rounded shadow-sm" style="border:1px solid #222">
                                @if (obj.Image==null||obj.Image=="")
                                {                              
                                <div>
                                        <img  src="@URL.ImageUserURL@URL.Scala" height=" 400px" width="100%" style="border-radius:5px; border:1px solid #bbb" />
                                </div>
                                }
                                else{
                                <div>
                                        <img  src="@URL.ImageUserURL@obj.Image" height=" 400px" width="100%"   alt="Card image cap">
                                </div>
                                }
                                <div class="card-body p-1 px-3 row">
                                    <div class="col-7 text-dark"><h4 class="float-left">@obj.Name</h4></div>
                                    <div class="col-12 p-1">
                                        <a asp-controller="User" asp-action="Details" asp-route-id="@obj.Id" class="btn btn-dark form-control btn-sm p-2" style="height:40px">Переглянути деталі</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }

                }
                else
                {
                    <p> Немає користувачів </p>
                }
            </div>
    </section>
</main>
</body>