@model IEnumerable<TrenersUser>
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Моя команда</h2>
        </div>
    </div>
    <br /><br />

    @if (Model.Count()>0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Ім'я користувача
                    </th>
                    <th>
                             Рік народження
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach(var obj in Model)
                {
                    <tr>
                        <td>
                            <a asp-controller="User" asp-route-Id="@obj.UnicKey" asp-action="Details" >
                                @obj.Name
                            </a>
                        </td>
                        <td>
                            @obj.Year
                        </td>
                        <td class="text-center">
                            <div class="w-75 btn-group" role="group">
                                 <a asp-controller="Trener" asp-route-Id="@obj.Id" asp-action="DeleteFromTeam" class=" btn btn-primary mx-2">
                               Видалити користувача
                                 </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p> No Users </p>
    }
</div>