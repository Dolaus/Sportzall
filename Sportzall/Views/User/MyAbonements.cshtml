@model Sportzall.Models.User
@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Your Abonements</h4>
    
    </hr>

    @if (Model.AbonementsUser.Count > 0)
    {


        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Abonements Name
                    </th>
                    <th>
                        Description
                    </th>
                    <th>
                        Price
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model.AbonementsUser)
                {           
                if(obj.IsPay==true)
                {
                    <tr>
                        <td>
                            @obj.Name
                        </td>
                        <td>
                             @obj.Description
                        </td>
                        <td>
                             @obj.Price
                        </td>
                        <td class="text-center">
                            <div class="w-75 btn-group" role="group">
                                 <a asp-controller="Abonement" asp-route-Id="@obj.Id" asp-action="DeleteUserAbonement" class=" btn btn-primary mx-2">
                                 Delete Abonement
                                 </a>
                                 
                            </div>
                        </td>
                    </tr>
                    }
                }
            </tbody>
        </table>
    }
    else
    {
        <p>You dont have abonements</p>
    }



</div>


