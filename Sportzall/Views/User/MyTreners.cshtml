@model IEnumerable<TrenersUser>
@{
    ViewData["Title"] = "Details";
}

<h1>Тренери</h1>

<div>
    <h4>Тренери</h4>
    
    </hr>
    <table class="table table-dark table-hover" style="width:100%">
            <thead>
                <tr>
                    <th>
                       Ім'я
                    </th>
                <th>
                    
                </th>
                </tr>
            </thead>
            <tbody>
            @if (Model.Count() > 0)
            {
                @foreach (var obj in Model)
                {
                    <tr>
                        <td>
                                     <a  asp-controller="User"   asp-action="Details" asp-route-id="@obj.UserId" >                         
                                        @obj.User.Name
                                        </a>
                          

                        </td>
                      
                        <td>
                           <div class="w-75 btn-group" role="group">
                                <a asp-controller="User" asp-action="SelectDayAddMyToRozklad" asp-route-trenersid="@obj.UserId" class=" btn btn-warning mx-2">
                                    Записатися
                                </a>
                            </div>
                        </td>
                        
                    </tr>
                }
            }
            else
            {
               <p>You dont have treners</p>
            }
                
            </tbody>
     </table>

        <br />
</div>


